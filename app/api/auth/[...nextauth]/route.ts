// Auth.js (next-auth v5) route handler for the App Router.
// This coexists with the Pages Router used for the UI pages.
import { handlers } from "../../../auth.config.ts";export const { GET, POST } = handlers;
